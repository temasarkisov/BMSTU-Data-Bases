CREATE TABLE Circuit(
    circuit_id INTEGER, 
    circuit_ref VARCHAR, 
    name VARCHAR,
    location VARCHAR,
    country VARCHAR,
    lat FLOAT,
    lng FLOAT,
    alt FLOAT, 
    PRIMARY KEY (circuit_id)
    );
