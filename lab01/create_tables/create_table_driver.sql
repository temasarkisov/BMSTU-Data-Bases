CREATE TABLE Driver(
    driver_id INTEGER, 
    driver_ref VARCHAR, 
    driver_number INTEGER,
    code VARCHAR(3),
    forename VARCHAR,
    surname VARCHAR,
    dob DATE,
    nationality VARCHAR, 
    PRIMARY KEY (driver_id)
    );
